# @logicommerce/domain-events

Versioned, privacy-minimized event envelopes for the transactional outbox.

Freight, billing, and payment events carry identifiers and bounded business
facts, never driver phone plaintext, signed object URLs, raw provider payloads,
card data, wallet secrets, or document contents.
