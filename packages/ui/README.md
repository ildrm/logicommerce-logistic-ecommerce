# @logicommerce/ui

Project-owned accessible UI primitives and semantic design tokens. Business
components consume tokens instead of hard-coded colors.

Operational timelines use semantic ordered lists and text labels in addition
to color. A reported-milestone timeline must not be styled or labelled as live
GPS.
