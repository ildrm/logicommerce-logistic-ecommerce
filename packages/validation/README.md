# @logicommerce/validation

Shared Zod schemas for identifiers, money, pagination, and tenant-safe input.

Freight inputs add mode-specific stops, cargo, coordinates, document metadata,
quote/booking transitions, check-ins, invoices, and payment-provider fields.
Provider webhook signature validation remains provider-specific API behavior.
